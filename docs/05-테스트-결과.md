# AutoFlow 백엔드 테스트 결과

## 테스트 환경

- Python 3.11+
- SQLite (aiosqlite)
- FastAPI + Uvicorn
- 테스트 일자: 2026-03-29

## 테스트 결과 요약

| 테스트 항목 | 상태 | 비고 |
|------------|------|------|
| 서버 시작 | ✅ 성공 | Health check 통과 |
| 데이터베이스 초기화 | ✅ 성공 | 모든 테이블 생성 |
| 시드 데이터 | ✅ 성공 | 데모 계정 및 마켓플레이스 아이템 |
| 인증 API | ✅ 성공 | 로그인/회원가입/토큰 검증 |
| 마켓플레이스 API | ✅ 성공 | 아이템 목록/상세 조회 |
| 채팅 API | ✅ 성공 | 대화 목록/읽지 않은 메시지 |
| 구매 API | ✅ 성공 | 구매 목록 조회 |
| 사용자 API | ✅ 성공 | 통계 조회 |

## 상세 테스트 결과

### 1. 로그인 테스트

```
POST /api/auth/login
Request: {"email": "buyer@demo.com", "password": "demo1234"}
Response: 200 OK
- User: 이구매자
- Token: 정상 발급
```

### 2. 현재 사용자 정보

```
GET /api/auth/me
Authorization: Bearer {token}
Response: 200 OK
- id: u2
- email: buyer@demo.com
- name: 이구매자
- role: buyer
```

### 3. 마켓플레이스 아이템 목록

```
GET /api/marketplace/items
Response: 200 OK
- total: 8
- 다운로드 순으로 정렬됨
```

### 4. 아이템 상세

```
GET /api/marketplace/items/1
Response: 200 OK
- title: 네이버 최저가 비교기 자동화
```

### 5. 대화 목록

```
GET /api/conversations
Response: 200 OK
- conversations: 1
- topic: 네이버 카드 자동화 v2
```

### 6. 읽지 않은 메시지 수

```
GET /api/conversations/unread-count
Response: 200 OK
- count: 1
```

### 7. 구매 목록

```
GET /api/purchases
Response: 200 OK
- active: []
- pending: []
- cancelled: []
```

### 8. 사용자 통계

```
GET /api/users/me/stats
Response: 200 OK
- templates_count: 0
- total_downloads: 0
- total_revenue: 0
- avg_rating: 0
- active_purchases: 0
```

## 발견된 이슈 및 해결

### 1. email-validator 누락

- **문제**: Pydantic EmailStr 타입 사용 시 email-validator 패키지 필요
- **해결**: requirements.txt에 email-validator==2.3.0 추가

### 2. bcrypt 버전 호환성

- **문제**: bcrypt 5.0.0 버전에서 `__about__` 속성 오류 및 72바이트 제한 문제
- **해결**: bcrypt==4.0.1로 다운그레이드

## 데모 계정

| 이메일 | 비밀번호 | 역할 |
|--------|----------|------|
| seller@demo.com | demo1234 | 판매자 |
| buyer@demo.com | demo1234 | 구매자 |

## 서버 실행 방법

```bash
cd backend
python -m venv venv
venv\Scripts\activate  # Windows
pip install -r requirements.txt
python run.py
```

서버가 `http://localhost:8000`에서 실행됩니다.

- API 문서: http://localhost:8000/docs
- ReDoc: http://localhost:8000/redoc
